class Solution:
    def hasDuplicate(self, nums: List[int]) -> bool:
        for i in range(len(list)):
            if i[i] == i:
                return True
        else:
            return False
        